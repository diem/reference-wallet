version: '3.4'
services:
  gateway:
    volumes:
      - ../gateway/nginx.conf:/etc/nginx/nginx.conf

  backend-web-server:
    environment:
      - COMPOSE_ENV="development"
    ports:
      - "5000"

  liquidity:
    environment:
      - COMPOSE_ENV="development"
    ports:
      - "5000"
